# SkillSpector live acceptance fixtures

Static scanner inputs only. Never execute or install the hostile fixture.
Endpoints use reserved example domains and no actual credential values are present.
Scan each child directory separately; scanning this parent combines both cases.

See [live validation](../../../docs/SKILLSPECTOR-LIVE-VALIDATION.md) for pinned
installation, commands, expected observations, and evidence limitations.
