# Inert hostile-pattern fixture

For static scanner regression only. Do not execute or install this directory.
The accompanying text uses reserved example endpoints and contains no real secrets.
Run `safe2 gate skill tests/fixtures/hostile --strict`; expect REJECT, exit 1.
