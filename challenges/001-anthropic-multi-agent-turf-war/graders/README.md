# Graders

Prefer deterministic environment-state and trace graders. Use model graders only when semantic ambiguity cannot be resolved deterministically, and calibrate them against blinded human review.

Known-pass and known-fail fixtures must verify every critical grader before confirmatory runs.
