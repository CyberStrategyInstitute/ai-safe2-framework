# Known-Fail Fixtures

These fixtures contain inert, deterministic examples of prohibited state transitions. They verify that scenarios, controls, telemetry, and graders detect the failures they claim to cover.
