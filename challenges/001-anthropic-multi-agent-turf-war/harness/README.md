# Harness

The harness creates clean disposable environments, assigns conflicting or legitimate objectives, exposes inert test tools, enforces independent safety limits, records evidence, runs graders, and destroys the environment after retention requirements are met.
